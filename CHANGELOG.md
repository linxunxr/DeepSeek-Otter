# 更新日志

面向用户的版本变更记录。发版前在此追加新版本段落（最新在最上），CI 生成更新清单时按
`## v<版本号>` 标记提取该版本段落写入 latest.json 的 notes，壳页面更新面板按 Markdown 渲染。

格式约定：`##` 版本标题（含日期）、`###` 分组、`-` 列表项、`**加粗**`、`` `行内码` ``；
壳页面只渲染这些语法，不要使用表格、链接、图片。

## v0.1.5（2026-09-19）

### 新增

- 控制中心新增两个入口：全局快捷键 `Ctrl+Alt+O`（任何应用前台可呼出；被其他程序占用时自动降级、不弹错误）与启动页按钮

## v0.1.4（2026-09-19）

### 修复

- **控制中心窗口打开后白屏**：本地页面改用应用内路径加载（v0.1.3 手拼绝对地址在安装版下不生效，托盘点开只见表框不见内容）

## v0.1.3（2026-09-19）

### 新增

- **Otter 控制中心**：托盘新增常驻前台入口，集中管理更新检查、版本信息与诊断导出；驻留期发现新版本会自动弹出。后续将陆续入驻模型与供应商、插件市场、Skill、迁移工具
- 应用图标：从占位方块换为品牌图标（蓝色渐变 + Otter 之 "O" + 水波）

## v0.1.2（2026-09-19）

### 更新体验

- 启动时自动检查新版本：**发现新版才提示**，已是最新或网络异常不打扰
- 关窗驻留期间每 24 小时自动检查一次，发现新版在托盘菜单显示"发现新版本"
- 更新面板展示当前版本对比、发布日期与更新说明
- 下载与安装仍由你手动确认，不会自动消耗流量或打断进行中的会话

## v0.1.1（2026-09-19）

### 内置 dsh 升级 0.1.2-rc.1 → 0.1.5-rc.2

- Web 右侧边栏：文档、代码、图片预览，多标签与分栏
- 会话中可上传任意类型文件，与图片混排展示
- 长会话打开、恢复与持续对话更流畅，内存占用更低
- Windows 上后台子进程不再弹出控制台窗口
- 出站网络请求遵循 `HTTP_PROXY` 等代理环境变量
- 安装包体积随依赖树增大：约 51 MB → 76 MB

### 注意

- 会话格式升级至 V3 且**不支持降级**：升级后请勿回滚安装旧版本，已迁移的会话将无法读取

## v0.1.0（2026-09-11）

### 首个发布版本

- DeepSeek Harness（dsh）的轻量桌面壳：窗口、托盘、单实例
- 关窗驻留不退出，后端"不用即停"，托盘点开即恢复
- dsh 完整依赖树离线打进安装包，首装零网络
- 双源热更新：Gitee（国内主）+ GitHub（兜底）
