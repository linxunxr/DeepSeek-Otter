# 更新日志

面向用户的版本变更记录。发版前在此追加新版本段落（最新在最上），CI 生成更新清单时按
`## v<版本号>` 标记提取该版本段落写入 latest.json 的 notes，壳页面更新面板按 Markdown 渲染。

格式约定：`##` 版本标题（含日期）、`###` 分组、`-` 列表项、`**加粗**`、`` `行内码` ``；
壳页面只渲染这些语法，不要使用表格、链接、图片。

## v0.1.12（2026-09-19）

### 新增

- **供应商可直接粘贴 API Key**：控制中心供应商编辑新增「API Key」栏，贴入 key 保存即可使用，无需再理解或配置环境变量；key 由 Otter 本地保管，启动后端时自动注入，不会写进任何 dsh 配置文件。原「API Key 环境变量名」保留为高级选项，两者填其一

## v0.1.11（2026-09-19）

### 修复

- **供应商配置不再依赖"保存时生成"的 patch 文件**：后端每次启动前都从配置源重新生成注入文件——配置文件在、注入文件丢失或过期时，重启后端即自动恢复生效（此前会出现"配置看着在、模型选择器里却选不到"的死结）；配置损坏时记录日志并降级启动（不带供应商，不影响基础使用）

## v0.1.10（2026-09-19）

### 修复

- **修复保存模型供应商配置后后端无法启动**：`--patch` 参数此前拼在 `web` 子命令之前，dsh 会拒绝启动（页面表现为一直"自动重连中"）；已改为正确顺序并在保存供应商配置后实测启动成功
- **修复后端启动失败时的无限重试**：自动重启计数此前被误清零，失败后每 2 秒无限重试刷日志；现在按 2s/4s/8s 重试 3 次后停止，并把窗口带回壳页面展示失败原因（不再滞留在死页面上转圈）
- **apiKeyEnv 保存时校验**：该字段要填环境变量名（如 `MY_GATEWAY_API_KEY`），此前误填 key 本体会导致后端启动即崩，现在保存时即拦截并提示正确填法
- **日志时间戳改本地时间**：此前为 UTC，比墙钟慢 8 小时，排障易误导

## v0.1.9（2026-09-19）

### 新增

- **会话历史导出**：迁移工具新增"导出会话"——Zcode 会话一键导出为 Markdown 归档（标题/目录/时间/对话正文），写入数据目录 `imported-sessions\`，可查阅可搜索（dsh 会话为私有格式且无官方导入通道，归档为安全形态，格式级迁移待上游开放）
- **供应商模板扩充至 11 家**：新增硅基流动、火山方舟（豆包）、OpenRouter、Ollama（本地）、xAI（Grok）
- **数据目录选择改弹窗**：迁移目标经系统目录选择器选取，不再手动输入路径

## v0.1.8（2026-09-19）

### 新增

- **模型供应商预置模板**：添加时一键填充 GLM（智谱）、Kimi（月之暗面）、MiniMax、通义千问、OpenAI、Anthropic 的地址与协议，模型 id 按需核对修改
- **数据目录可迁移**：dsh 用户数据（会话/插件/Skill，默认在 C 盘 `~/.dsh`）可一键迁移到其他盘符——自动复制现有数据并重启后端，原目录保留

## v0.1.7（2026-09-19）

### 新增

- **控制中心四页功能上线**：
  - **模型与供应商**：接入多个模型供应商（OpenAI 兼容网关、Anthropic 协议等），配置模型列表与上下文窗口，新会话默认模型可切换；保存后一键重启后端生效
  - **插件市场**：按包名安装/卸载 dsh 插件（npm 体系），查看已装列表
  - **Skill**：管理用户全局技能（`~/.dsh/skills`，增删免重启生效），支持**从 Zcode 勾选导入**
  - **迁移工具**：一键迁移 Zcode 全局指令 `AGENTS.md` 到 dsh（已存在自动备份后覆盖）

## v0.1.6（2026-09-19）

### 修复

- **更新安装报"Error opening file for writing: node.exe"**：安装前未停 dsh 后端，运行中的后端进程锁住安装目录文件导致 NSIS 写入失败。现在下载完成后自动停止后端再安装（下载期间会话不受影响）

## v0.1.5（2026-09-19）

### 新增

- 控制中心新增两个入口：全局快捷键 `Ctrl+Alt+O`（任何应用前台可呼出；被其他程序占用时自动降级、不弹错误）与启动页按钮

## v0.1.4（2026-09-19）

### 修复

- **控制中心窗口打开后白屏**：本地页面改用应用内路径加载（v0.1.3 手拼绝对地址在安装版下不生效，托盘点开只见表框不见内容）

## v0.1.3（2026-09-19）

### 新增

- **Otter 控制中心**：托盘新增常驻前台入口，集中管理更新检查、版本信息与诊断导出；驻留期发现新版本会自动弹出。后续将陆续入驻模型与供应商、插件市场、Skill、迁移工具
- 应用图标：从占位方块换为品牌图标（蓝色渐变 + Otter 之 "O" + 水波）

## v0.1.2（2026-09-19）

### 更新体验

- 启动时自动检查新版本：**发现新版才提示**，已是最新或网络异常不打扰
- 关窗驻留期间每 24 小时自动检查一次，发现新版在托盘菜单显示"发现新版本"
- 更新面板展示当前版本对比、发布日期与更新说明
- 下载与安装仍由你手动确认，不会自动消耗流量或打断进行中的会话

## v0.1.1（2026-09-19）

### 内置 dsh 升级 0.1.2-rc.1 → 0.1.5-rc.2

- Web 右侧边栏：文档、代码、图片预览，多标签与分栏
- 会话中可上传任意类型文件，与图片混排展示
- 长会话打开、恢复与持续对话更流畅，内存占用更低
- Windows 上后台子进程不再弹出控制台窗口
- 出站网络请求遵循 `HTTP_PROXY` 等代理环境变量
- 安装包体积随依赖树增大：约 51 MB → 76 MB

### 注意

- 会话格式升级至 V3 且**不支持降级**：升级后请勿回滚安装旧版本，已迁移的会话将无法读取

## v0.1.0（2026-09-11）

### 首个发布版本

- DeepSeek Harness（dsh）的轻量桌面壳：窗口、托盘、单实例
- 关窗驻留不退出，后端"不用即停"，托盘点开即恢复
- dsh 完整依赖树离线打进安装包，首装零网络
- 双源热更新：Gitee（国内主）+ GitHub（兜底）
