{
  "name": "deepseek-otter",
  "version": "0.1.13",
  "description": "DeepSeek Harness (dsh) 的轻量桌面发行版",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "typecheck": "tsc --noEmit",
    "fetch-runtime": "node scripts/fetch-runtime.mjs",
    "test:unit": "cd src-tauri && cargo test",
    "smoke": "node scripts/smoke-test.mjs",
    "smoke:fresh": "node scripts/smoke-test.mjs --fresh",
    "smoke:build": "node scripts/build-smoke.mjs",
    "test": "pnpm typecheck && pnpm test:unit"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/plugin-dialog": "^2.7.3",
    "@tauri-apps/plugin-process": "^2.3.1",
    "@tauri-apps/plugin-updater": "^2.11.0"
  }
}
